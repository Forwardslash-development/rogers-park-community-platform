# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: auth.spec.ts >> Authentication Flow >> should complete full login flow
- Location: tests/auth.spec.ts:19:3

# Error details

```
Test timeout of 30000ms exceeded.
```

```
Error: page.waitForURL: Test timeout of 30000ms exceeded.
=========================== logs ===========================
waiting for navigation to "/dashboard" until "load"
============================================================
```

# Page snapshot

```yaml
- generic [ref=e3]:
  - banner [ref=e4]:
    - navigation [ref=e5]:
      - link "Home" [ref=e6] [cursor=pointer]:
        - /url: /
      - link "Login" [ref=e7] [cursor=pointer]:
        - /url: /login
      - link "Signup" [ref=e8] [cursor=pointer]:
        - /url: /signup
  - main [ref=e9]:
    - generic [ref=e11]:
      - heading "Welcome Back" [level=1] [ref=e12]
      - paragraph [ref=e13]: Login to your account
      - generic [ref=e14]:
        - generic [ref=e15]:
          - generic [ref=e16]: Email
          - textbox "Email" [active] [ref=e17]
        - generic [ref=e18]:
          - generic [ref=e19]: Password
          - textbox "Password" [ref=e20]: TestPass123
        - button "Login" [ref=e21] [cursor=pointer]
      - paragraph [ref=e22]:
        - text: Don't have an account?
        - link "Sign up" [ref=e23] [cursor=pointer]:
          - /url: /signup
```

# Test source

```ts
  1   | import { test, expect } from '@playwright/test';
  2   | 
  3   | test.describe('Authentication Flow', () => {
  4   |   test('should complete full signup flow', async ({ page }) => {
  5   |     await page.goto('/signup');
  6   |     
  7   |     const timestamp = Date.now();
  8   |     await page.fill('input[name="display_name"]', 'Test User');
  9   |     await page.fill('input[name="email"]', `test${timestamp}@example.com`);
  10  |     await page.fill('input[name="password"]', 'TestPass123');
  11  | 
  12  |     await page.click('button[type="submit"]');
  13  |     
  14  |     await page.waitForURL('/dashboard');
  15  |     await expect(page.locator('h2')).toContainText('Welcome, Test User!');
  16  |     await expect(page.locator('dd').first()).toContainText(`test${timestamp}@example.com`);
  17  |   });
  18  | 
  19  |   test('should complete full login flow', async ({ page }) => {
  20  |     await page.goto('/signup');
  21  |     const timestamp = Date.now();
  22  |     const email = `login${timestamp}@example.com`;
  23  |     
  24  |     await page.fill('input[name="display_name"]', 'Login Test');
  25  |     await page.fill('input[name="email"]', email);
  26  |     await page.fill('input[name="password"]', 'TestPass123');
  27  |     await page.click('button[type="submit"]');
  28  |     
  29  |     await page.waitForURL('/dashboard');
  30  | 
  31  |     // Logout
  32  |     await page.click('button[type="submit"]');
  33  |     await page.waitForURL('/');
  34  | 
  35  |     // Now login
  36  |     await page.goto('/login');
  37  | 
  38  |     await page.fill('input[name="email"]', email);
  39  |     await page.fill('input[name="password"]', 'TestPass123');
  40  |     await page.click('button[type="submit"]');
  41  | 
> 42  |     await page.waitForURL('/dashboard');
      |                ^ Error: page.waitForURL: Test timeout of 30000ms exceeded.
  43  |     await expect(page.locator('h2')).toContainText('Welcome, Login Test!');
  44  |   });
  45  | 
  46  |   test('should reject invalid login credentials', async ({ page }) => {
  47  |     await page.goto('/login');
  48  | 
  49  |     await page.fill('input[name="email"]', 'nonexistent@example.com');
  50  |     await page.fill('input[name="password"]', 'WrongPassword');
  51  |     await page.click('button[type="submit"]');
  52  | 
  53  |     await page.waitForTimeout(1000);
  54  |     await expect(page).toHaveURL('/login');
  55  |   });
  56  | 
  57  |   test('should reject duplicate email on signup', async ({ page }) => {
  58  |     // Use a fixed email to ensure we can test duplicate
  59  |     const email = 'duplicate-test@example.com';
  60  |     
  61  |     // First signup
  62  |     await page.goto('/signup');
  63  |     await page.fill('input[name="display_name"]', 'First User');
  64  |     await page.fill('input[name="email"]', email);
  65  |     await page.fill('input[name="password"]', 'TestPass123');
  66  |     await page.click('button[type="submit"]');
  67  |     
  68  |     // Wait to see if it redirects or shows error
  69  |     await page.waitForTimeout(2000);
  70  |     
  71  |     // If it succeeded, we're on dashboard - logout
  72  |     if (page.url().includes('/dashboard')) {
  73  |       await page.click('button[type="submit"]');
  74  |       await page.waitForURL('/');
  75  |     }
  76  | 
  77  |     // Try to signup with same email again
  78  |     await page.goto('/signup');
  79  |     await page.fill('input[name="display_name"]', 'Second User');
  80  |     await page.fill('input[name="email"]', email);
  81  |     await page.fill('input[name="password"]', 'TestPass123');
  82  |     await page.click('button[type="submit"]');
  83  | 
  84  |     // Should show error OR stay on signup page
  85  |     await page.waitForTimeout(2000);
  86  |     
  87  |     // Should not redirect to dashboard
  88  |     expect(page.url()).toContain('/signup');
  89  |     
  90  |     // Should show error if error element exists
  91  |     const errorCount = await page.locator('.error').count();
  92  |     if (errorCount > 0) {
  93  |       await expect(page.locator('.error')).toBeVisible();
  94  |     }
  95  |   });
  96  | 
  97  |   test('should logout successfully', async ({ page }) => {
  98  |     await page.goto('/signup');
  99  |     const timestamp = Date.now();
  100 |     
  101 |     await page.fill('input[name="display_name"]', 'Logout Test');
  102 |     await page.fill('input[name="email"]', `logout${timestamp}@example.com`);
  103 |     await page.fill('input[name="password"]', 'TestPass123');
  104 |     await page.click('button[type="submit"]');
  105 |     
  106 |     await page.waitForURL('/dashboard');
  107 | 
  108 |     // Logout
  109 |     await page.click('button[type="submit"]');
  110 |     await page.waitForURL('/');
  111 | 
  112 |     // Check for nav links
  113 |     await expect(page.locator('nav a[href="/signup"]')).toBeVisible();
  114 |     await expect(page.locator('nav a[href="/login"]')).toBeVisible();
  115 |   });
  116 | 
  117 |   test('should protect dashboard route when not logged in', async ({ page }) => {
  118 |     await page.goto('/dashboard');
  119 |     await page.waitForURL('/login');
  120 |   });
  121 | 
  122 |   test('should show validation errors for invalid email', async ({ page }) => {
  123 |     await page.goto('/signup');
  124 | 
  125 |     await page.fill('input[name="display_name"]', 'Test');
  126 |     await page.fill('input[name="email"]', 'notanemail');
  127 |     await page.fill('input[name="password"]', 'TestPass123');
  128 |     
  129 |     await page.click('button[type="submit"]');
  130 |     
  131 |     await page.waitForTimeout(500);
  132 |     expect(page.url()).toContain('/signup');
  133 |   });
  134 | });
  135 | 
```